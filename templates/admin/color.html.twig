{% extends 'panelAdmin.html.twig' %}
{% block title %}
    {{ parent() }} Gestion des couleurs
{% endblock %}

{% block titre %}
    Gestion des couleurs
{% endblock %}



{% block body %}


    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_widget(form.colorCode) }}


{#    <div class="container p-3 mb-3 ms-1 ">#}
{#        <span class="align-bottom p-3">#}
{#        Color generator <input type="color" id="colorInput" style=" width: 70px; border: none; cursor:pointer">#}
{#        </span>#}
{#        <input type="button" class="btn btn-sm btn-dark" style="border-radius:50px" id="btn" value="appliquer" onclick="maCouleur()">#}
{#        Code : <input type="text" class="mt-3" id="box" style="width: 100px; border-radius:50px">#}
{#    </div>#}
{#    <script type="text/javascript">#}
{#        function maCouleur(){#}
{#            let color = document.getElementById("colorInput").value;#}
{#            document.getElementById("box").value = color;#}
{#        }#}
{#    </script>#}



    {{ form_row(form.Enregistrer) }}
    {{ form_end(form) }}





    <table class="table mt-3">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Couleur</th>
            <th scope="col">Aperçu</th>
            <th scope="col">Actions</th>
        </tr>
        </thead>

        <tbody>


        {% for color in colors %}
            <tr>
                <th scope="row">{{ color.id }}</th>
                <td>{{ color.title}}</td>
                <td><div style="background-color:{{ color.colorCode }};border: 2px solid grey;width: 30px; min-height: 30px" ></div></td>
                <td>
                    <a href="{{ path ('editColor', { id:color.id })  }}"><img src=" {{ asset('update.png') }} " alt="modifier"></a>
                    <a href="{{ path ('deleteColor', { id:color.id })  }}" onClick="return confirm('Etes-vous sur de vouloir supprimer cette couleur ?')"><img src=" {{ asset('delete.png') }} " alt="supprimer"></a>
                </td>
            </tr>
        {% endfor %}

        </tbody>
    </table>



{% endblock %}